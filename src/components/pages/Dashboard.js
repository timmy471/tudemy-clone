import React from 'react';
import { withRouter } from 'react-router-dom';

const Dashboard = () => {
    return (
        <div>
            <h2>Dashboard</h2>
        </div>
    )
}

export default withRouter(Dashboard)
